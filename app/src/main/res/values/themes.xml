<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CoreConnectionCounter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CoreConnectionCounter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/button_home</item>
    </style>

    <!-- 共通縮尺スタイル（FrameLayout 用） - 値は @fraction/number_set_scale で管理 -->
    <style name="ScaledFrame">
        <item name="android:scaleX">@fraction/number_set_scale</item>
        <item name="android:scaleY">@fraction/number_set_scale</item>
    </style>

</resources>
